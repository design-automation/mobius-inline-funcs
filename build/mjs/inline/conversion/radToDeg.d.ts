/**
 * Converts radians to degrees.
 * ```
 * radToDeg(PI)         // 180
 * radToDeg(PI / 2)     // 90
 * ```
 * @param rad A number (representing radians).
 * @returns A number (representing degrees).
 */
export declare function radToDeg(rad: number | number[]): number | number[];
